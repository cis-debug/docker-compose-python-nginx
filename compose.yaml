services:
  api:
    build:
      context: ./app
    container_name: python-api
    expose:
      - "5000"

  nginx:
    image: nginx:alpine
    container_name: nginx-front
    ports:
      - "8080:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api
